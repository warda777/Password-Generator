public class App {
    public static void main(String[] args) {
        // instantiate the GUI
        PasswordGenerator passwordGeneratorGUI = new PasswordGenerator();

        // make the GUI visible
        passwordGeneratorGUI.setVisible(true);
    }
}
